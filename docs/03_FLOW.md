# 系统流程

## 文本模式

1. 用户输入文本
2. 调用 LLM
3. 返回结果
4. 显示UI

---

## 语音模式（后续）

1. 点击语音按钮
2. 申请麦克风权限
3. Vosk STT 离线识别
4. 识别文本回填输入框
5. 用户确认后发送给 LLM
6. 返回结果

说明：当前阶段先做 STT 输入，不自动发送；TTS 播放后续再接入。

---

## LLM处理流程

用户输入 → Prompt包装 → LLM推理 → 返回结构化文本
